Before running the program, make sure that you have cassava (CSV parsing and encoding library) installed.
If not, you can install it via the command line with the following commands:

1. Get latest package list:
cabal update

2. Install the cassave library
cabal install cassava